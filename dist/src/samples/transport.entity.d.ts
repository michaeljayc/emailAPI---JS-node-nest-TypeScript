export declare class Transport {
    transport: string;
    reason: string;
    expenses: number;
    time_start: string;
    time_end: string;
    constructor(transport: string, reason: string, expenses: number, time_start: string, time_end: string);
}
